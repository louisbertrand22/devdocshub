# Example environment variables for DevDocsHub Frontend
# Copy this file to .env.dev, .env.prod, or .env.local and fill in the values

# API Base URL
# Local dev: http://localhost:8000
# Production: https://api.devdocshub.com
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Application Name
NEXT_PUBLIC_APP_NAME=DevDocsHub

# NextAuth Secret (optional, if using NextAuth)
# Generate a strong secret: openssl rand -base64 32
# NEXTAUTH_SECRET=changeme

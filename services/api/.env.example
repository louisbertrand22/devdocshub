# Example environment variables for DevDocsHub API
# Copy this file to .env.dev or .env.prod and fill in the values

# Database Configuration
# SQLite for local dev: sqlite:///./devdocshub.db
# PostgreSQL: postgresql+psycopg://user:password@host:port/database
DATABASE_URL=sqlite:///./devdocshub.db

# JWT Authentication
# Generate a strong secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=changeme
JWT_EXPIRE_MINUTES=60
JWT_REFRESH_EXPIRE_DAYS=30

# MeiliSearch Configuration
MEILI_HTTP=http://localhost:7700
MEILI_INDEX=docs

# S3/MinIO Configuration (optional, for file storage)
# S3_ENDPOINT=http://localhost:9000
